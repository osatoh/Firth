<%# A form rather than a link: following the original marks the article read, which must not happen on GET. %>
<%= button_to label, visit_article_path(article),
      form: { target: "_blank", data: { turbo: false }, class: "inline" }, class: %>
