<% content_for :title, t(".title") %>

<div class="w-full">
  <h1 class="text-2xl font-bold mb-6"><%= t(".title") %></h1>

  <% if @articles.empty? %>
    <p class="text-gray-500"><%= t(".empty") %></p>
  <% else %>
    <ul class="divide-y divide-gray-200">
      <% @articles.each do |article| %>
        <li class="py-4 min-w-0">
          <%= link_to article.title, article, class: "font-medium hover:underline" %>
          <%= link_to t(".original"), article.url, target: "_blank", rel: "noopener noreferrer",
                class: "text-sm text-blue-600 hover:underline" %>
          <p class="text-sm text-gray-500">
            <%= render "meta", article: %>
          </p>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>
